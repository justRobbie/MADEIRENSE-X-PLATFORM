import {
    type ComponentProps,
} from "react";

// ***************************************************************************************************************

function BackOfficeResortsPage(props: ComponentProps<"main">) {
    return <main {...props}>
        resorts
    </main>
};

export default BackOfficeResortsPage;