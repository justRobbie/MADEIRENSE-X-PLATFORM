import {
    type ComponentProps,
} from "react";

// ***************************************************************************************************************

function BackOfficeBookingsPage(props: ComponentProps<"main">) {
    return <main {...props}>
        booked
    </main>
};

export default BackOfficeBookingsPage;