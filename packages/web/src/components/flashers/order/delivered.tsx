import ConfettiAnimation from "components/animations/confetti";

// ***************************************************************************************************************

function DELIVERED_ORDER_FLASHER () {
    return <main>
        <ConfettiAnimation size='l' pieces={100} defaultShape='rect' />
    </main>
};

export default DELIVERED_ORDER_FLASHER;